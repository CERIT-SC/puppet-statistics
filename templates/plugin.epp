<%- | String  $name,
      Integer $interval = 300,
      Hash    $options = {},
| -%>
<LoadPlugin <%= $name %>>
   Interval <%= $interval %>
</LoadPlugin>

<Plugin <%= $name %>>
  <% $options.each |$key, $value| { -%>
     <%= $key %> = <%= $value %>
  <% } -%>
  </Plugin>
